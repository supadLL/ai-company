[project]
name = "ai-company-api"
version = "0.1.0"
description = "Local API relay for the AI Company desktop app"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "httpx>=0.27.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic-settings>=2.4.0",
  "sqlmodel>=0.0.22"
]

[tool.uvicorn]
factory = false
