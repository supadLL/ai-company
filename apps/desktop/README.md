# AI Company Desktop

Tauri + React shell for the AI Company workspace.

## Run Web Shell

```bash
npm install
npm run dev
```

## Run Tauri Shell

```bash
npm install
npm run tauri dev
```

The local API service should run separately on `http://localhost:8787` during MVP development.
